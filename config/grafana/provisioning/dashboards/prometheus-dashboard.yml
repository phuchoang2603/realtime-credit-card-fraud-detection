apiVersion: 1

providers:
  - name: "Prometheus"
    folder: ""
    type: file
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
    editable: true
    allowUiUpdates: true
