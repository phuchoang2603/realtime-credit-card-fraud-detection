service:
  type: LoadBalancer
  # Set default IP for proxmox rke2 cluster
  # spec:
  #   loadBalancerIP: "10.69.3.3"

tlsStore:
  default:
    defaultCertificate:
      secretName: wildcard-tls

ports:
  web:
    redirections:
      entryPoint:
        to: websecure
        scheme: https
        permanent: true
